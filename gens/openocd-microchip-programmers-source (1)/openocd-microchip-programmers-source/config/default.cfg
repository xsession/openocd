# Default Docker runtime config.
# Replace this file with your board/interface config or override command in compose.
# Example for ST-Link + STM32 target:
# source [find interface/stlink.cfg]
# transport select hla_swd
# source [find target/stm32f4x.cfg]

bindto 0.0.0.0
gdb_port 3333
telnet_port 4444
tcl_port 6666

echo "No board config selected. Mount config/default.cfg or pass: -f interface/xxx.cfg -f target/yyy.cfg"
